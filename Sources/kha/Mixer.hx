package kha;

class Mixer {
	/*private static var channels: Array<Sound>;
	
	public static function init(): Void {
		channels = new Array<Sound>();
		for (i in 0...7) channels[i] = null;
	}
	
	public static function play(sound: Sound): Void {
		for (i in 0...7) {
			if (channels[i] == null || channels[i].isFinished()) {
				channels[i] = sound;
				sound.playOnChannel(i);
			}
		}
	}*/
}